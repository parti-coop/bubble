%a{id: 'post-panel-anchor'}
.posts-panel

  %h3.section-title.background-mint 의견 적기

  %a{id: 'post-form-anchor'}
  %p.description
    너의 의견을 남겨줘.
    %br
    법안에 어떤 내용이 담겼으면 하는지,
    %br
    우리 당이 어떻게 활동했으면 좋겠는지 모두 다 괜찮아
    %i !
  = render 'posts/form'

  %a{id: 'posts-anchor'}
  .post-page
    .post-search
      .post-search-info
        - if params[:q].present?
          %i.fa.fa-search
          검색된 글 #{@posts.total_count}개
          = link_to root_path(anchor: 'posts-anchor'), class: 'cancel-search' do
            검색 취소
        - else
          총 #{@posts.total_count}개
      .post-search-form
        = form_tag root_path(anchor: 'posts-anchor'), method: :get, id: 'post-search-form' do
          .form-group
            = search_field_tag 'q', params[:q], class: 'search-query', maxlength: 20, autocorrect: "off", autocapitalize: "none"
            %button.btn{:type => "submit"}
              %i.fa.fa-search
    = render 'posts/post_page', board_slug: Post::BOARD_SLUG_PARTY_BUILDING


