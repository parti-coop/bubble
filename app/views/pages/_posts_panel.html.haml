%a{id: 'post-panel-anchor'}
.posts-panel
  %h3.section-title 적는 당

  %p.description
    너의 의견을 남겨줘.
    %br
    법안에 어떤 내용이 담겼으면 하는지,
    %br
    우리 당이 어떻게 활동했으면 좋겠는지 모두 다 괜찮아
    %i !

  - unless user_signed_in?
    .login.clearfix
      .btn-login-wrapper
        = link_to user_omniauth_authorize_path(provider: :facebook), class: 'btn btn-default btn-login btn-login-facebook' do
          %i.fa.fa-facebook
          페이스북 로그인
      .btn-login-wrapper.text-center
        = link_to user_omniauth_authorize_path(provider: :twitter), class: 'btn btn-default btn-login btn-login-twitter' do
          %i.fa.fa-twitter
          트위터 로그인
      .btn-login-wrapper.text-right
        = link_to user_omniauth_authorize_path(provider: :naver), class: 'btn btn-default btn-login btn-login-naver' do
          %i.fa.fa-twitter
          네이버 로그인

    %p.description
      이미 당원이라면 소셜 미디어로 쉽게 로그인
      %i !
  = render 'posts/form'

  %a{id: 'posts-anchor'}
  .post-page
    = render 'posts/post_page', board_slug: Post::BOARD_SLUG_PARTY_BUILDING

  .post-search
    %form#post-search-form
      .form-group
        %input.search-query{:type => "text"}
        %button.btn{:type => "submit"}
          %i.fa.fa-search

