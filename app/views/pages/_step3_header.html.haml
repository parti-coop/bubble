%section#header
  .container.container-xs-space
    .top-menu
      %span
        = link_to '홈', root_path
      %span.pull-right
        - if user_signed_in?
          = link_to '로그아웃', destroy_user_session_path, method: :delete
        - else
          = link_to '당원가입 / 로그인', root_path(anchor: 'anchor-join')
%section#steps
  .container
    .project-steps
      .menu-temp{style: 'color:#fff'}
        step3 메뉴
